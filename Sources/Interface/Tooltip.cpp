#include "Tooltip.h"
/*
// -----------------------------------------------------------------
// Name : Tooltip
//  Constructor
// -----------------------------------------------------------------
Tooltip::Tooltip() : guiDocument()
{
}

// -----------------------------------------------------------------
// Name : ~Tooltip
//  Destructor
// -----------------------------------------------------------------
Tooltip::~Tooltip()
{
#ifdef DBG_VERBOSE1
  printf("Begin destroy Tooltip\n");
#endif
#ifdef DBG_VERBOSE1
  printf("End destroy Tooltip\n");
#endif
}

// -----------------------------------------------------------------
// Name : setText
// -----------------------------------------------------------------
void Tooltip::setText(char * sText)
{
  deleteAllComponents();
  guiLabel * pLbl = new guiLabel();
  pLbl->init(sText, m_DefaultFontId, m_DefaultTextColor, "", 2, 2, 0, 0, getDisplay());
  setDimensions(pLbl->getWidth() + 4, pLbl->getHeight() + 4);
  addComponent(pLbl);
}
*/
